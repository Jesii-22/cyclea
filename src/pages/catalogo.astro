---
import Layout from '../layouts/Layout.astro';
---

<Layout>
  <section class="catalog py-20 px-10">
    <h1 class="text-4xl font-bold text-center mb-10">Catálogo de Materiales</h1>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      <div class="card bg-white p-6 rounded-lg shadow-lg hover:shadow-xl transition">
        <h3 class="text-xl font-bold">Tela de Algodón Roja</h3>
        <p>Cantidad: 50kg | Empresa: EcoTextil</p>
        <button class="mt-4 bg-green-500 text-white px-4 py-2 rounded">Conectar</button>
      </div>
      <!-- Añade más tarjetas -->
    </div>
  </section>
</Layout>

<script>
  import { gsap } from 'gsap';
  gsap.fromTo('.card', { opacity: 0, scale: 0.8 }, { opacity: 1, scale: 1, duration: 1, stagger: 0.3 });
</script>