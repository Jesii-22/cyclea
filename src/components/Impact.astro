---
// Componente para la sección de impacto
---

<section class="impact py-20 px-10 text-center bg-gradient-to-r from-blue-400 to-gray-100">  <!-- Fondo gradiente visible -->
  <div class="max-w-4xl mx-auto text-center">  <!-- Centrado -->
    <h2 class="text-4xl font-bold mb-8 text-gray-800 text-center">Impacto del Reciclaje Textil</h2>
    <p class="text-lg text-gray-600 mb-6 text-center">Cada año, millones de toneladas de residuos textiles terminan en vertederos. Cyclea transforma esto en oportunidades, conectando oferta y demanda para un futuro sostenible.</p>
    <div class="stats flex justify-center space-x-12 mt-10 text-center">  <!-- Estadísticas centradas -->
      <div class="stat">
        <h3 class="text-3xl font-bold text-green-600 text-center">500+</h3>
        <p class="text-gray-500 text-center">Empresas conectadas</p>
      </div>
      <div class="stat">
        <h3 class="text-3xl font-bold text-blue-600 text-center">1000+</h3>
        <p class="text-gray-500 text-center">Productos reutilizados</p>
      </div>
    </div>
    <a href="/registro-empresas" class="mt-8 inline-block bg-green-500 text-white px-6 py-3 rounded-lg hover:bg-green-600 transition text-center">Únete como Empresa</a>
  </div>
</section>

<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';
  gsap.registerPlugin(ScrollTrigger);
  gsap.fromTo('.impact h2, .impact p, .stat', { opacity: 0, y: 30 }, { opacity: 1, y: 0, duration: 1, stagger: 0.2, scrollTrigger: { trigger: '.impact', start: 'top 80%' } });
</script>